var fork = require('child_process').fork
var path = require('path')

var msgs = []

describe("Pitchfork CLI (Command Line Tool)", function(){

  it("should return truncated json")

  it("should return usage when no flags are passed")

})